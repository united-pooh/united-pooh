name: Generate Snake

on:
  schedule:
    - cron: "0 16 * * *" 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Generate Snake
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: united-pooh
          github_token: ${{ secrets.PAT }}
          
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#00ff00&color_dots=#1a1a2e,#ff006e,#8338ec,#3a86ff,#06ffa5
            dist/github-contribution-grid-snake-dark.svg?color_snake=#fcee0a&color_dots=#161b22,#4cc9f0,#4361ee,#7209b7,#f72585
            
      - name: Push to output branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: output
          publish_dir: dist
